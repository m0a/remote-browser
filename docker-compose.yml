services:
  browser:
    build: .
    ports:
      - "3000"
      - "9223"
      - "6080"
    environment:
      - CDP_PORT=9222
      - PORT=3000
    volumes:
      - ./chrome-profile:/app/chrome-profile
    shm_size: "2gb"
    restart: unless-stopped
