services:
  browser:
    build: ./cef-browser
    ports:
      - "3000"
      - "9222"
    environment:
      - CDP_PORT=9222
      - PORT=3000
      - START_URL=https://www.google.com
    shm_size: "2gb"
    restart: unless-stopped
